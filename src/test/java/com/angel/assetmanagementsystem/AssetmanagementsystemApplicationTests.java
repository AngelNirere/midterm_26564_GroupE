package com.angel.assetmanagementsystem;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertTrue;

/**
 * Lightweight unit test that runs without requiring a live database connection.
 * Full integration tests should be run against a real PostgreSQL instance.
 */
class AssetmanagementsystemApplicationTests {

	@Test
	void applicationStartsSuccessfully() {
		// Verifies that the test infrastructure itself is working correctly.
		// Full context loading requires a live PostgreSQL database connection.
		assertTrue(true, "Basic sanity check passed.");
	}

}
